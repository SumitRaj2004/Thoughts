<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.1.1/css/all.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/script.js" defer></script>
    <title>Update thought</title>
</head>
<body>
    {{>navbar}}
    <section class="updateThought-section">
        <div class="small-container">
            <form action="/dashboard/thought/update/{{thought.id}}?_method=PATCH" class="updateThought-form" method="POST">
                <input type="text" name="title" id="" placeholder="Your Title" value="{{thought.title}}">
                <textarea name="content" placeholder="Your Thought">{{thought.content}}</textarea>
                <div class="updateThoughtForm-bottom">
                    {{#if publicScope}}
                    {{!-- if the s scope if public --}}
                        <select name="scope" class="scope">
                            <option value="public" selected>Public</option>
                            <option value="private">Private</option>
                        </select>

                        {{else}}
                        {{!-- if the scope if private --}}
                        <select name="scope" class="scope">
                        <option value="private" selected>Private</option>
                        <option value="public">Public</option>
                    </select>
                    {{/if}}
                    <input type="submit" value="Update" class="primary-btn">
                </div>
            </form>
        </div>
    </section>
</body>
</html>